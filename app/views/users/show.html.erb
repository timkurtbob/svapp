<%= render 'components/navbar_user_show', user: @user %>
<div class="contatiner">
  <div class="profile-box">
    <% if @user.pic %>
      <%= cl_image_tag(@user.pic, width: 160, height: 160, crop: "fill", radius: "max") %>
    <% else %>
      <i class="far fa-user-circle fa-7x"></i>
    <% end %>
    <h2 class="red-font bold-font margin-top-30"><%= @user.first_name %> <%= @user.second_name %></h2>
    <h4 class="red-font bold-font"><%= @user.school.name.split.map(&:capitalize)*' ' %></h4>
    <h4 class="red-font bold-font"><%= @user.role.capitalize %></h4>
    <div class="margin-30">
      <p><strong>Email:</strong> <%= @user.email %></p>
      <p><strong>Telefon:</strong> <%= @user.phone %></p>
    </div>
  </div>
</div>
