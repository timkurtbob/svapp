function addComment() {
  const add_comment_are = document.querySelector('.new-comment-area')
  const html = "<%= j render 'components/comment', comment: @comment %>";
  add_comment_are.insertAdjacentHTML('afterend', html)
  const inputField = document.getElementById('comment_text');
  inputField.value = '';
}

addComment()
