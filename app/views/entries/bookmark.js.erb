function toggleBookmark() {
  // toggling the icon
  html = '<%= j render "components/bookmark", entry: @entry %>'
  const bookmark_container = document.getElementById('<%="entry_#{@entry.id}"%>');
  bookmark_container.innerHTML = html
}

toggleBookmark()
