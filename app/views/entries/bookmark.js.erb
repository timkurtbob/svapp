function toggleBookmark() {
  // toggling the icon
  html = '<%= j render "components/bookmark", entry: @entry %>'
  const bookmarkContainer = document.getElementById('<%="entry_#{@entry.id}"%>');
  bookmarkContainer.innerHTML = html
}

toggleBookmark()
